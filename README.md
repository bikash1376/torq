Add 3 more tools - one that generates full quiz with 10 questions and answer on any topic user asks displays them in canvas with next button, a interactive game that takes any math topic and calls gemini api with a specific prompt so that it could display the html css js render- like create an minimal yet interactive on {topic} with answers and all but hidden to the student until he finishes or loses (in case). thirdly add a flashcards tool that takes a topic that generates 5 flashcards and dsiplays in the canvas. when user clicks new chat make sure the canvas is clear